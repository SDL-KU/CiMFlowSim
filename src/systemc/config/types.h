#ifndef CONFIG_TYPES_H
#define CONFIG_TYPES_H

// Forward declarations for the types that are defined in pipeline_simulator.h
// This header provides forward declarations to avoid circular dependencies

struct CNNParams;
struct HWConfig;
struct TilingConfig;

#endif  // CONFIG_TYPES_H